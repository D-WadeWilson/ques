<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.yz.questionnaire.dao.AnswercardDao">

    <resultMap type="com.yz.questionnaire.entity.Answercard" id="AnswercardMap">
        <result property="id" column="id" jdbcType="INTEGER"/>
        <result property="userid" column="userId" jdbcType="INTEGER"/>
        <result property="paperid" column="paperId" jdbcType="INTEGER"/>
        <result property="questionid" column="questionId" jdbcType="INTEGER"/>
        <result property="answer" column="answer" jdbcType="VARCHAR"/>
    </resultMap>

    <!--查询单个-->
    <select id="queryById" resultMap="AnswercardMap">
        select
          id, userId, paperId, questionId, answer
        from questionnaire.answercard
        where id = #{id}
    </select>

    <!--查询指定行数据-->
    <select id="queryAllByLimit" resultMap="AnswercardMap">
        select
          id, userId, paperId, questionId, answer
        from questionnaire.answercard
        limit #{offset}, #{limit}
    </select>

    <!--通过实体作为筛选条件查询-->
    <select id="queryAll" resultMap="AnswercardMap">
        select
          id, userId, paperId, questionId, answer
        from questionnaire.answercard
        <where>
            <if test="id != null">
                and id = #{id}
            </if>
            <if test="userid != null">
                and userId = #{userid}
            </if>
            <if test="paperid != null">
                and paperId = #{paperid}
            </if>
            <if test="questionid != null">
                and questionId = #{questionid}
            </if>
            <if test="answer != null and answer != ''">
                and answer = #{answer}
            </if>
        </where>
    </select>

    <!--新增所有列-->
    <insert id="insert" keyProperty="id" useGeneratedKeys="true">
        insert into questionnaire.answercard(userId, paperId, questionId, answer)
        values (#{userid}, #{paperid}, #{questionid}, #{answer})
    </insert>

    <!--通过主键修改数据-->
    <update id="update">
        update questionnaire.answercard
        <set>
            <if test="userid != null">
                userId = #{userid},
            </if>
            <if test="paperid != null">
                paperId = #{paperid},
            </if>
            <if test="questionid != null">
                questionId = #{questionid},
            </if>
            <if test="answer != null and answer != ''">
                answer = #{answer},
            </if>
        </set>
        where id = #{id}
    </update>

    <!--通过主键删除-->
    <delete id="deleteById">
        delete from questionnaire.answercard where id = #{id}
    </delete>

</mapper>